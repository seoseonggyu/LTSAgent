@* LayoutComponentBase를 상속받아 레이아웃으로 사용 (@Body 자리에 페이지가 렌더링됨) *@
@inherits LayoutComponentBase

<!-- 공통 레이아웃 컨테이너 (모든 페이지를 감싸는 외곽 틀) -->
<div class="h-screen flex flex-col overflow-hidden">
    @Body
</div>